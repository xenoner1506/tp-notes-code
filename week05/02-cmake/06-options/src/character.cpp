#include "character.hpp"

void Character::SetHealth(int points) {
    health_points = points;
}

void Character::SetMana(int points) {
    mana_points = points;
}

void Character::SetAgility(int points) {
    agility_points = points;
}